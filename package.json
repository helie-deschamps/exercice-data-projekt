{
  "name": "exercice-data-projekt",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "api:dev": "yarn workspace api start",
    "frontend:dev": "yarn workspace frontend dev",

    "frontend:build": "yarn workspace frontend build",
    
    "api:preview": "yarn workspace api start",
    "frontend:preview": "yarn workspace frontend preview"
  }
}