{
  "name": "api",
  "version": "1.0.0",
  "description": "API REST simulée pour l'exercice FAQ - Recrutement stagiaire fullstack",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "start:delay": "json-server --watch db.json --port 3001 --delay 500",
    "start:custom": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "keywords": [
    "json-server",
    "api",
    "mock",
    "faq",
    "rest"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
